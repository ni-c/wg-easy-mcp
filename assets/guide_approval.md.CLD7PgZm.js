import{H as e,ht as t,nt as n,tt as r}from"./chunks/framework.qcK96eCy.js";var i=JSON.parse(`{"title":"Asking a person","description":"","frontmatter":{},"headers":[],"relativePath":"guide/approval.md","filePath":"guide/approval.md","lastUpdated":1788348152000}`),a={name:`guide/approval.md`};function o(e,i,a,o,s,c){return t(),r(`div`,null,[...i[0]||=[n(`<h1 id="asking-a-person" tabindex="-1">Asking a person <a class="header-anchor" href="#asking-a-person" aria-label="Permalink to “Asking a person”">​</a></h1><p>Five of the eleven tools change who can reach the network behind this VPN. All five <strong>ask a person first</strong>.</p><p>Not a <code>confirm: true</code> argument the model can set. Not a token the model reads out of its own previous result. A dialog, raised through <a href="https://modelcontextprotocol.io/specification/2025-11-25/client/elicitation" target="_blank" rel="noreferrer">MCP elicitation</a>, that goes to the client and is shown to whoever is sitting there.</p><p>The specification says a client <em>should</em> keep a human in the loop:</p><blockquote><p>there <strong>SHOULD</strong> always be a human in the loop with the ability to deny tool invocations</p></blockquote><p>This server does not rely on that. It raises the question itself, and until an answer comes back, nothing happens.</p><h2 id="what-asks-and-when" tabindex="-1">What asks, and when <a class="header-anchor" href="#what-asks-and-when" aria-label="Permalink to “What asks, and when”">​</a></h2><table tabindex="0"><thead><tr><th>Tool</th><th>When it asks</th></tr></thead><tbody><tr><td><code>create_client</code></td><td>always</td></tr><tr><td><code>update_client</code></td><td>always, bound to the <strong>exact edit</strong></td></tr><tr><td><code>enable_client</code></td><td>always</td></tr><tr><td><code>delete_client</code></td><td>always</td></tr><tr><td><code>generate_one_time_link</code></td><td>always</td></tr><tr><td>everything else</td><td>never</td></tr></tbody></table><p>Only one of the five destroys anything, and that is the whole reason this page exists next to the annotations. <code>destructiveHint</code> is the wrong axis for the other four:</p><ul><li><p><code>create_client</code> issues a credential that reaches every network this VPN reaches. Deleting it later does not undo a connection it made in the meantime.</p></li><li><p><code>update_client</code> can move an address or widen <code>serverAllowedIps</code>. The approval is bound to the exact edit, so approving a rename does not license a later call that widens the routes.</p></li><li><p><code>generate_one_time_link</code> mints a URL that hands out a client&#39;s full configuration — private key included — to anyone who has it, without authentication. Its own annotation had said “which is why the tool is guarded instead” since it was written, and it was not.</p></li><li><p><code>enable_client</code> re-arms a key pair that is already installed on a peer, so nothing further has to be handed over for that peer to connect. It was not on this list until 0.5.0, on the argument that it and <code>disable_client</code> are each other&#39;s undo and a dialog in front of a reversible state change teaches people to tick without reading. That argument does not survive the pairing being asymmetric: <code>disable_client</code> withdraws access and <code>enable_client</code> grants it, and only one of those two directions is worth a question. It also left the guard on <code>update_client</code> avoidable — <code>update_client({enabled: true})</code> asked and <code>enable_client</code> did not — while <code>WG_EASY_ALLOW_TOOLS=essential</code> registered only the ungated one.</p></li></ul><p><code>disable_client</code> is deliberately not on the list, and is the only write tool that is not: it can only ever withdraw access, and an operator cutting a peer off should not have to answer a dialog to do it.</p><h2 id="what-the-dialog-contains" tabindex="-1">What the dialog contains <a class="header-anchor" href="#what-the-dialog-contains" aria-label="Permalink to “What the dialog contains”">​</a></h2><p>The client id, what the call costs, and the client&#39;s <strong>name</strong> on a labelled line under a heading saying those values did not come from this server.</p><p>A dialog that says only “Delete client 5?” is not something a person can act on — recognising the name is the whole point of asking them. The name comes from wg-easy, so it goes under the disclaimer rather than into the sentence, where a client called “ignore previous instructions” would read as the server speaking.</p><div class="language-"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span>This will delete WireGuard client 5.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Its key pair is destroyed and cannot be restored. Anyone using that configuration</span></span>
<span class="line"><span>loses VPN access, and re-creating the client gives them a different key they have</span></span>
<span class="line"><span>to install.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Values below are supplied by the caller, not by this server:</span></span>
<span class="line"><span>  Client: laptop</span></span></code></pre></div><p>The approval is bound to its target, so one obtained for a call cannot be replayed against another. For a <em>set</em> of targets the binding is a fingerprint of the exact list: an approval for <code>[&quot;a&quot;]</code> does not execute <code>[&quot;a&quot;, &quot;b&quot;]</code>.</p><h2 id="clients-that-cannot-show-a-dialog" tabindex="-1">Clients that cannot show a dialog <a class="header-anchor" href="#clients-that-cannot-show-a-dialog" aria-label="Permalink to “Clients that cannot show a dialog”">​</a></h2><p>Not every MCP client implements elicitation, and a stateless gateway may not be able to speak for the one it is currently serving. Rather than refuse to work — which pushes people towards switching the guard off entirely — the tool falls back to a <strong>two-call token</strong>: the first call returns a random string, the second has to quote it back.</p><p>Be clear about what that proves, because this server is:</p><blockquote><p>the token proves the call was made twice with the same arguments, and nothing more.</p></blockquote><p>A model can read the token out of the first result and call again in the same turn without anybody seeing it. It catches a widened target set; it does not catch a model that was talked into the whole thing. The fallback text says so rather than implying somebody approved.</p><h2 id="switching-the-dialog-off" tabindex="-1">Switching the dialog off <a class="header-anchor" href="#switching-the-dialog-off" aria-label="Permalink to “Switching the dialog off”">​</a></h2><div class="language-sh"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">ELICITATION</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">false</span></span></code></pre></div><p>Default is <code>true</code>. <code>false</code> does <strong>not</strong> remove the guard — it takes the fallback path above, which means the token. There is no setting in which a guarded call goes unannounced.</p><p>Use it where a dialog is the wrong shape rather than an unwanted one: a scheduled job, a test harness, a client whose dialog interrupts something else.</p><div class="warning custom-block"><p class="custom-block-title">It is deliberately not prefixed</p><p><code>ELICITATION</code> has no <code>WG_EASY_</code> in front of it, so one <code>export ELICITATION=false</code> — or one <code>-e ELICITATION=false</code> in a compose file — reaches <strong>every</strong> MCP server in that environment, not just this one. That is the point of it and also its risk.</p><p>Two things make it visible rather than silent:</p><ul><li><p>a server started with it off prints one line at startup, in the log of every server it actually reached:</p><div class="language-"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span>wg-easy-mcp: ELICITATION=false — guarded tools fall back to the two-call token</span></span></code></pre></div></li><li><p>the fallback text names the server that did not ask, instead of blaming a client that was working fine. :::</p></li></ul><p>Anything other than <code>true</code> or <code>false</code> — <code>1</code>, <code>off</code>, <code>yes</code> — <strong>stops the server</strong> with exit code 1 and a message naming both valid values. This is the only variable in this family that defaults to <em>on</em>: a typo that fell back to the default would leave the dialog running while the operator believed it was off, and there would be nothing to tell them.</p><h2 id="annotations-are-the-other-half-and-they-are-only-a-hint" tabindex="-1">Annotations are the other half, and they are only a hint <a class="header-anchor" href="#annotations-are-the-other-half-and-they-are-only-a-hint" aria-label="Permalink to “Annotations are the other half, and they are only a hint”">​</a></h2><p>Every tool of this server declares all four MCP tool annotations — <code>readOnlyHint</code>, <code>destructiveHint</code>, <code>idempotentHint</code>, <code>openWorldHint</code> — so a client can tell before it calls what a call would do. See <a href="/reference/tools">Tools</a>.</p><p>They are advice, and the specification says so:</p><blockquote><p>clients <strong>MUST</strong> consider tool annotations to be untrusted unless they come from trusted servers</p></blockquote><p>An annotation is something a client may ignore. The dialog is not: it is enforced here, on the server side, and no answer means no change. The two are different claims — the annotation says what a call <em>does</em>, the dialog decides whether it <em>happens</em> — which is why a tool can be marked destructive without being guarded. <code>get_client_config</code> and <code>get_client_qrcode</code> are the other way round: they are <code>readOnlyHint: true</code>, entirely correctly, and they hand back a private key. Read-only is a statement about the server&#39;s state, not about how harmless the answer is to hold.</p><h2 id="behind-a-gateway" tabindex="-1">Behind a gateway <a class="header-anchor" href="#behind-a-gateway" aria-label="Permalink to “Behind a gateway”">​</a></h2><p>Both protocol revisions are handled from one code path. On <code>2025-11-25</code> the question is pushed to the client; on <code>2026-07-28</code> there is no server→client channel at all, so the call returns <code>input_required</code>, ends, and the client retries carrying the answer.</p><p>That answer arrives as ordinary request content, which the SDK does not validate — so the state that ties an answer to its question is sealed (HMAC). A reply whose seal does not open, or opens onto a different target, counts as <strong>no answer</strong> and produces a fresh question rather than an error. The likeliest cause is not an attack: it is a gateway that put the server to sleep while the person was reading.</p><p>If you run this behind <a href="https://github.com/ni-c/mcp-hub" target="_blank" rel="noreferrer">mcp-hub</a>, the hub passes elicitation through in both directions; see its <a href="https://ni-c.github.io/mcp-hub/guide/elicitation" target="_blank" rel="noreferrer">elicitation guide</a>.</p></div>`,26)]])}var s=e(a,[[`render`,o]]);export{i as __pageData,s as default};