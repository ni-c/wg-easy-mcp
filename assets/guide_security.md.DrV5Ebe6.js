import{_t as e,it as t,n,rt as r}from"./chunks/framework.D9LADW4G.js";var i=JSON.parse(`{"title":"Security","description":"","frontmatter":{},"headers":[],"relativePath":"guide/security.md","filePath":"guide/security.md","lastUpdated":1788812764000}`),a={name:`guide/security.md`};function o(n,i,a,o,s,c){return e(),r(`div`,null,[...i[0]||=[t(`<h1 id="security" tabindex="-1">Security <a class="header-anchor" href="#security" aria-label="Permalink to “Security”">​</a></h1><p>This page is the prose version of the repository&#39;s <a href="https://github.com/ni-c/wg-easy-mcp/blob/main/SECURITY.md" target="_blank" rel="noreferrer">SECURITY.md</a>. For reporting a vulnerability, use <a href="https://github.com/ni-c/wg-easy-mcp/security/advisories/new" target="_blank" rel="noreferrer">private vulnerability reporting</a> — never a public issue.</p><h2 id="what-the-credentials-grant" tabindex="-1">What the credentials grant <a class="header-anchor" href="#what-the-credentials-grant" aria-label="Permalink to “What the credentials grant”">​</a></h2><p>The server authenticates with a wg-easy <strong>admin</strong> login. wg-easy has no read-only role, so there is no configuration in which this server holds less than full VPN administration. Concretely, anything able to read this process&#39;s environment can:</p><ul><li>create, disable and delete peers,</li><li>download any peer&#39;s configuration file and QR code, both of which contain that peer&#39;s <strong>private key</strong>,</li><li>mint one-time links that let an unauthenticated visitor download a config.</li></ul><p>Treat the process environment, the MCP client&#39;s config file and the tool output as three copies of the same secret.</p><h2 id="who-decides-what-gets-called" tabindex="-1">Who decides what gets called <a class="header-anchor" href="#who-decides-what-gets-called" aria-label="Permalink to “Who decides what gets called”">​</a></h2><p>The MCP client does. This server enforces the confirmation handshake and the redaction rules described below, but it cannot tell a legitimate request from one a model was talked into. Connect it only to clients you would trust with the VPN itself.</p><h2 id="guard-rails-in-the-server" tabindex="-1">Guard rails in the server <a class="header-anchor" href="#guard-rails-in-the-server" aria-label="Permalink to “Guard rails in the server”">​</a></h2><h3 id="five-tools-ask-a-person" tabindex="-1">Five tools ask a person <a class="header-anchor" href="#five-tools-ask-a-person" aria-label="Permalink to “Five tools ask a person”">​</a></h3><p><code>create_client</code>, <code>update_client</code>, <code>enable_client</code>, <code>delete_client</code> and <code>generate_one_time_link</code> put the question to a <strong>person</strong> before they act, through MCP elicitation — a dialog the model cannot answer on its behalf, and which nothing proceeds without.</p><p>Only one of the five destroys anything. The other four are on the list because <code>destructiveHint</code> is the wrong axis for what they do:</p><ul><li><code>create_client</code> issues a credential that reaches every network behind the VPN.</li><li><code>update_client</code> can move an address or widen <code>serverAllowedIps</code>. Its approval is bound to the <strong>exact edit</strong>, so approving a rename does not license a later call that widens the routes.</li><li><code>enable_client</code> re-arms a key pair that is already installed on a peer. If <code>disable_client</code> is the reversible revocation this server recommends — and its catalogue says it is — then enabling is the undo of a revocation. It was ungated until 0.5.0, which also made the guard on <code>update_client</code> avoidable: <code>update_client({enabled: true})</code> asked and <code>enable_client</code> did not, so the same state change was guarded or not depending on which tool the model picked.</li><li><code>generate_one_time_link</code> mints a URL that hands out a client&#39;s full configuration — private key included — to anyone who has it, without authentication.</li></ul><p><code>disable_client</code> deliberately stays ungated. It only ever withdraws access, and putting a dialog between an operator and cutting off a peer is the wrong place for one.</p><p>Where the client cannot show a dialog, all five fall back to a random 128-bit token bound to the same target and valid for <strong>five minutes</strong>; only a second call carrying that exact token acts. A boolean would not do: a model can set one by itself on the first call, including when it has been steered there by text it read somewhere.</p><p>Be clear about what the token proves, because this server is: <strong>the call was made twice with the same arguments, and nothing more.</strong> A model can read it out of the first result and quote it back in the same turn. The fallback text says so rather than implying somebody approved, and names whether it was the client that could not be asked or the operator who switched the dialog off with <code>ELICITATION=false</code>.</p><p>The prompt shows the client&#39;s <strong>name</strong> on a labelled line under a heading saying those values did not come from this server. A dialog that says only &quot;Delete client 5?&quot; is not something a person can act on; a name in the server&#39;s own sentence would read as the server vouching for it.</p><p>See <a href="/guide/approval">Asking a person</a>.</p><h3 id="key-material-is-redacted" tabindex="-1">Key material is redacted <a class="header-anchor" href="#key-material-is-redacted" aria-label="Permalink to “Key material is redacted”">​</a></h3><p>Everything the wg-easy API hands back is filtered before it is returned. A key is sensitive by what it <strong>ends in</strong>, matched on the name with <code>_</code> and <code>-</code> removed and lower-cased: <code>password</code>, <code>passwordHash</code>, <code>passwd</code>, <code>passphrase</code>, <code>secret</code>, <code>token</code>, <code>apiKey</code>, <code>privateKey</code>, <code>preSharedKey</code>, plus anything starting with <code>totp</code>. The value is replaced with <code>[redacted]</code> at every nesting level.</p><p>The suffix rule is there because an exact list is only as good as the backend&#39;s naming. <code>GET /api/admin/general</code> carries the argon2 hash of the metrics token as <code>metricsPassword</code>, and <code>password</code> matched <code>password</code> and not that — so until 0.6.0 the hash reached the model through a tool whose description promises the opposite. Every <code>&lt;prefix&gt;Secret</code> wg-easy invents next is covered now without anybody having to notice it.</p><p><code>key</code> is deliberately <strong>not</strong> a suffix. It would take <code>publicKey</code> — which is how a peer is identified, and is public — and every <code>*_key</code> identifier with it.</p><p>That covers two different secrets. <code>get_server_info</code> reads the admin endpoints, which carry the WireGuard <strong>server</strong> private key. <code>get_client</code> reads a single client, and wg-easy returns that client&#39;s <strong>own</strong> private key and pre-shared key in full — while <code>list_clients</code> does not, which is what made the leak easy to miss. Until the integration suite found it, <code>get_client</code> passed the key straight through. A key that reaches the model is in the transcript, where it outlives any decision to stop using it.</p><p>The filter is applied to both, rather than to the one endpoint that happens to carry the key today.</p><p>One-time links are redacted on the read path too. <code>GET /api/client</code> carries the link token on every row, and <code>GET /cnf/&lt;token&gt;</code> returns the whole configuration with <strong>no login at all</strong> — so <code>list_clients</code> used to hand out a working download URL for any client whose link had not yet expired. The token is replaced; <code>expiresAt</code> is not, because knowing that a link is live is exactly what a listing is for.</p><p>Note the deliberate asymmetry: <code>get_client_config</code> and <code>get_client_qrcode</code> return private keys <strong>unredacted</strong>, and <code>generate_one_time_link</code> returns the link token, because handing a peer its configuration is the point of those tools. Their descriptions say so. The difference is that somebody asked for it.</p><h3 id="read-only-mode-suppresses-key-disclosure" tabindex="-1">Read-only mode suppresses key disclosure <a class="header-anchor" href="#read-only-mode-suppresses-key-disclosure" aria-label="Permalink to “Read-only mode suppresses key disclosure”">​</a></h3><p><code>WG_EASY_READ_ONLY=true</code> registers <code>list_clients</code>, <code>get_client</code> and <code>get_server_info</code>, and nothing else.</p><p><code>get_client_config</code> and <code>get_client_qrcode</code> are reads — nothing on the instance changes — and they are <strong>not</strong> in that set, because what they read is a client&#39;s <code>PrivateKey</code> in the clear. Read-only mode is the one coarse switch an operator has for putting this server in front of a less trusted session, and a mode that leaves key disclosure standing is not the mode its name promises: <code>list_clients</code> followed by <code>get_client_config</code> yields one ready-to-use VPN configuration per peer, in the transcript, with no confirmation anywhere.</p><p>They are left out of <code>WG_EASY_ALLOW_TOOLS=essential</code> for the same reason. Where a session should also hand out configurations, name the tool:</p><div class="language-"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span>WG_EASY_ALLOW_TOOLS=essential,get_client_config</span></span></code></pre></div><p>This is the rule the catalogue already applies to <code>delete_client</code> — the variant that cannot be taken back has to be named explicitly — applied to disclosure rather than to destruction. Before 0.5.0 both tools counted as read tools, so <code>WG_EASY_READ_ONLY</code> changed nothing about them.</p><h3 id="what-binds-an-approval-and-what-makes-it-stale" tabindex="-1">What binds an approval, and what makes it stale <a class="header-anchor" href="#what-binds-an-approval-and-what-makes-it-stale" aria-label="Permalink to “What binds an approval, and what makes it stale”">​</a></h3><p>Where a client cannot show a dialog, a guarded tool hands back a <code>confirm_token</code> and acts only on a second call carrying it. Where a client <em>can</em>, the reply comes back sealed (HMAC) and carries the resource key of the operation it answered.</p><p>Both mechanisms bind an answer to <strong>one operation with one set of arguments</strong>. A seal on its own does not prove the answer is <em>recent</em>: a sealed <code>requestState</code> that opens onto an operation opens onto it whenever it is replayed, and the library says so.</p><p>That path is reachable here. <code>src/index.ts</code> serves through <code>serveStdio</code>, whose opening exchange selects <code>2025-11-25</code> or <code>2026-07-28</code> per connection, and on the later revision the dialog <em>is</em> the return value — the sealed state travels out through the client and comes back with the answer.</p><div class="warning custom-block"><p class="custom-block-title">This section used to say the opposite</p><p>Until 0.6.0 it argued that the server &quot;does not set <code>supportedProtocolVersions</code>, so it takes the SDK&#39;s default list, which ends at <code>2025-11-25</code>&quot;. That stopped being true when the entry point moved from a hand-wired <code>StdioServerTransport</code> to <code>serveStdio</code>, which negotiates both eras — and the sentence stayed. A claim in a security document that names a class or a protocol revision is only as good as the last time somebody checked it against the source.</p></div><p>What closes the gap is a mechanism, not the surroundings: <code>mcp-approval</code> ≥ 0.8.1 puts a nonce in every sealed state and <strong>spends it the first time an answer arrives</strong> — accepted or declined. The same state presented again counts as no answer at all and produces a fresh question, exactly as the <code>confirm_token</code> path consumes its token. The token path is single-use through <code>ConfirmationStore</code> and expires after five minutes.</p><p>Two things are still worth writing down, because a future change could remove them:</p><ul><li>The record of spent states and the 32-byte sealing key both live <strong>in the process</strong>. A restart forgets them — along with the session they belonged to.</li><li>A deployment that served the two halves of one flow from two processes with a shared key would have neither guarantee. This is a stdio server spawned per session, so that shape does not arise.</li></ul><h3 id="upstream-content-is-marked-untrusted" tabindex="-1">Upstream content is marked untrusted <a class="header-anchor" href="#upstream-content-is-marked-untrusted" aria-label="Permalink to “Upstream content is marked untrusted”">​</a></h3><p>Client names, DNS entries and endpoints are free-form strings chosen by whoever administers the VPN. Everything the wg-easy API returns is prefixed with an explicit untrusted-data marker telling the model to treat the block as data to report rather than instructions to follow, and is capped at 60 000 characters — measured on the text that is actually emitted — so a single oversized field cannot flood the context.</p><p>Those strings are also <strong>cleaned</strong>: C0 and C1 control characters, DEL and the BiDi override and isolate characters are removed from every value and every field name in a record. An ESC begins a terminal escape sequence in whatever renders the transcript, and a right-to-left override reorders the line around it — a client name is an odd place to find either.</p><p>Two answers are exempt and stay byte-exact, because they have to work as files: <code>get_client_config</code> and <code>get_client_qrcode</code>. A control character in one of those is reported in a <code>warning</code> field instead of being removed.</p><h3 id="nothing-the-instance-sends-is-taken-on-trust" tabindex="-1">Nothing the instance sends is taken on trust <a class="header-anchor" href="#nothing-the-instance-sends-is-taken-on-trust" aria-label="Permalink to “Nothing the instance sends is taken on trust”">​</a></h3><p>A JSON body is read with a TypeScript cast, which checks nothing, and the SDK validates every answer against the tool&#39;s output schema before it leaves. Those two facts meet badly: an <code>id</code> spelled as a string, a <code>1e999</code> in <code>transferRx</code> — <code>Infinity</code> after <code>JSON.parse</code>, which <code>z.number()</code> refuses — or a numeric <code>name</code> in one record used to answer the <strong>whole</strong> <code>list_clients</code> with <code>Output validation error</code> and no cause.</p><p>Every field the schema types is now read with a check of that exact type at the boundary, and a value that fails it is left out rather than passed on. A list entry that is not a record at all is left out and <strong>counted</strong> in <code>skipped</code>, not dropped in silence.</p><h3 id="transport" tabindex="-1">Transport <a class="header-anchor" href="#transport" aria-label="Permalink to “Transport”">​</a></h3><ul><li>Requests carry a <strong>15-second timeout</strong> and <code>redirect: &#39;error&#39;</code> — following a redirect would hand the <code>Authorization</code> header to whatever host it points at.</li><li>A successful body is refused above <strong>8 MiB</strong> — a declared <code>content-length</code> before a byte is read, otherwise the reader is cancelled at the ceiling. The timeout does not cover this: it is spent once the headers arrive, so a body that never ends was a process that never answered again.</li><li>The <strong>status is read before the body</strong>, and a failed one is read under its own 64 KiB ceiling that cuts rather than refuses — so a <code>401</code> behind a reverse proxy&#39;s login page is still a <code>401</code> with a credential hint.</li><li>A <strong>refused login is repeated from memory for ten seconds</strong> rather than retried. Every tool call carries the admin credentials, so every call is a login attempt, and <code>401</code> is the answer a model reads as transient. Only <code>401</code>; a <code>403</code> is a permission, not a guess.</li><li><code>WG_EASY_INSECURE_TLS</code> is a <strong>scoped undici dispatcher</strong>, so relaxed certificate validation applies to the wg-easy connection only and never process-wide.</li><li>A URL carrying embedded credentials is rejected at startup rather than logged, and only its origin and path are kept — a query or fragment would otherwise be glued in front of every request path.</li><li>Credentials are removed from <code>process.env</code> after the config is read.</li></ul><h3 id="error-output" tabindex="-1">Error output <a class="header-anchor" href="#error-output" aria-label="Permalink to “Error output”">​</a></h3><p>Upstream error bodies are labelled <code>(untrusted text from the instance)</code>, stripped of control characters and cut at 200 characters; HTML error pages — the usual output of a reverse proxy or WAF — are dropped entirely instead of being pasted into the model&#39;s context. The label and the cut matter because an error result is built from a message, and no result budget measures one.</p><h3 id="diagnostics-do-not-echo-values" tabindex="-1">Diagnostics do not echo values <a class="header-anchor" href="#diagnostics-do-not-echo-values" aria-label="Permalink to “Diagnostics do not echo values”">​</a></h3><p>A startup message that says &quot;got <code>&lt;value&gt;</code>&quot; prints whatever was pasted into the wrong variable, to stderr, which is the MCP client&#39;s log. <code>WG_EASY_URL</code> sits one line above <code>WG_EASY_PASSWORD</code> in every compose file, and <code>ELICITATION</code> is unprefixed and in the same block.</p><p>So the &quot;not a valid URL&quot; message names no value; the non-http(s) message names no <strong>scheme</strong>, because a hexadecimal key with a colon after it is a valid URL whose scheme is the key; and <code>ELICITATION</code> quotes only a short word-shaped value (<code>/^[A-Za-z0-9_-]{1,12}$/</code>, so a genuine typo is still shown) and describes anything else by its length alone.</p><h2 id="deployment-recommendations" tabindex="-1">Deployment recommendations <a class="header-anchor" href="#deployment-recommendations" aria-label="Permalink to “Deployment recommendations”">​</a></h2><ul><li>Keep the wg-easy admin UI reachable only from trusted networks. The MCP server uses the same URL and inherits exactly that exposure.</li><li>Give the server its own admin account where wg-easy supports multiple users, and rotate its password when you revoke access.</li><li>Pass credentials through the client&#39;s <code>env</code> block — not a command line, where they land in shell history and <code>ps</code>.</li><li>Prefer an internal CA over <code>WG_EASY_INSECURE_TLS</code>.</li></ul><h2 id="supply-chain" tabindex="-1">Supply chain <a class="header-anchor" href="#supply-chain" aria-label="Permalink to “Supply chain”">​</a></h2><p>Releases are published to npm via <a href="https://docs.npmjs.com/trusted-publishers" target="_blank" rel="noreferrer">Trusted Publishing</a> with <a href="https://docs.npmjs.com/generating-provenance-statements" target="_blank" rel="noreferrer">provenance</a> — no long-lived token exists to leak. Container images ship an SBOM and <code>provenance: mode=max</code>. CI runs <code>npm audit</code>, CodeQL, a Trivy scan of the image and <code>actions/dependency-review-action</code> — which checks what a pull request <em>adds</em>, rather than the tree as it stands — on every push and once a week.</p><p>Both jobs that hold an OIDC token install with <code>--ignore-scripts</code>, so no dependency&#39;s lifecycle hook runs while a credential that can publish this package is available, and <code>mcp-publisher</code> is pinned to a release tag with its published sha256 checked rather than fetched from <code>releases/latest</code>. The runtime image contains no npm, corepack or yarn at all.</p>`,59)]])}var s=n(a,[[`render`,o]]);export{i as __pageData,s as default};