import{H as e,ht as t,nt as n,tt as r}from"./chunks/framework.qcK96eCy.js";var i=JSON.parse(`{"title":"Configuration","description":"","frontmatter":{},"headers":[],"relativePath":"guide/configuration.md","filePath":"guide/configuration.md","lastUpdated":1788348125000}`),a={name:`guide/configuration.md`};function o(e,i,a,o,s,c){return t(),r(`div`,null,[...i[0]||=[n(`<h1 id="configuration" tabindex="-1">Configuration <a class="header-anchor" href="#configuration" aria-label="Permalink to “Configuration”">​</a></h1><p>Everything is configured through environment variables; there is no config file. The full table lives in the <a href="/reference/environment">environment reference</a> — this page covers the decisions behind them.</p><h2 id="the-url" tabindex="-1">The URL <a class="header-anchor" href="#the-url" aria-label="Permalink to “The URL”">​</a></h2><p><code>WG_EASY_URL</code> is the base URL of the wg-easy web UI, without a trailing path:</p><div class="language-"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span>https://vpn.example.com:51821</span></span></code></pre></div><p>Trailing slashes are stripped. The server validates the URL at startup and <strong>exits</strong> — rather than warning — in three cases:</p><table tabindex="0"><thead><tr><th>Rejected</th><th>Why</th></tr></thead><tbody><tr><td>Anything <code>new URL()</code> cannot parse</td><td>A malformed URL turns request paths into guesswork</td></tr><tr><td>A scheme other than <code>http</code>/<code>https</code></td><td>Nothing else can carry the API</td></tr><tr><td>Embedded credentials (<code>user:pass@</code>)</td><td>They would be logged at startup and prefixed onto every request</td></tr></tbody></table><p>Plain <code>http</code> to a non-loopback host only produces a warning, because it is a legitimate setup when the connection already runs inside a tunnel. It is still worth reading that warning: with plain http, both the Basic Auth credentials and every WireGuard private key the server returns travel in the clear.</p><h2 id="credentials" tabindex="-1">Credentials <a class="header-anchor" href="#credentials" aria-label="Permalink to “Credentials”">​</a></h2><p><code>WG_EASY_USERNAME</code> and <code>WG_EASY_PASSWORD</code> are the wg-easy <strong>web UI login</strong> of an admin account. Two consequences:</p><ul><li><strong>2FA must be off</strong> for that account. The API speaks Basic Authentication only, and a TOTP-protected account returns <code>HTTP 401</code> on every call.</li><li>The credentials are <strong>deleted from <code>process.env</code></strong> once the config is loaded, so a later crash dump or a child process cannot read them back out.</li></ul><div class="warning custom-block"><p class="custom-block-title">There is no read-only account</p><p>wg-easy admin credentials grant full VPN administration. This server has no way to hand out less than that — see <a href="/guide/security">Security</a>.</p></div><h2 id="self-signed-certificates" tabindex="-1">Self-signed certificates <a class="header-anchor" href="#self-signed-certificates" aria-label="Permalink to “Self-signed certificates”">​</a></h2><p><code>WG_EASY_INSECURE_TLS=true</code> accepts certificates that do not validate. It is implemented as a <strong>scoped undici dispatcher</strong> used only for the wg-easy connection, not <code>NODE_TLS_REJECT_UNAUTHORIZED</code>, so nothing else the process does loses certificate validation.</p><p>It is still the second-best answer. Prefer issuing the wg-easy certificate from an internal CA and trusting that CA on the machine running the server; then leave this unset.</p><h2 id="starting-without-credentials" tabindex="-1">Starting without credentials <a class="header-anchor" href="#starting-without-credentials" aria-label="Permalink to “Starting without credentials”">​</a></h2><p>If any of the three required variables is missing, the server <strong>still starts</strong>. It completes the MCP handshake, lists all eleven tools, and fails every actual call with the same setup instructions it printed on stderr.</p><p>That is deliberate: registries and sandbox inspectors start the server with no environment at all to enumerate its tools. Exiting at startup would leave the listing empty.</p><h2 id="where-the-values-live" tabindex="-1">Where the values live <a class="header-anchor" href="#where-the-values-live" aria-label="Permalink to “Where the values live”">​</a></h2><p>Pass them through your MCP client&#39;s <code>env</code> block, as shown in <a href="/guide/clients">Connecting clients</a>. Avoid putting the password on a command line — it lands in shell history and in <code>ps</code> output — and avoid committing it to a config file that is under version control.</p><h2 id="turning-the-approval-dialog-off" tabindex="-1">Turning the approval dialog off <a class="header-anchor" href="#turning-the-approval-dialog-off" aria-label="Permalink to “Turning the approval dialog off”">​</a></h2><p><code>create_client</code>, <code>update_client</code>, <code>delete_client</code> and <code>generate_one_time_link</code> ask a person through MCP elicitation before they act. <code>ELICITATION=false</code> takes them to the two-call token instead. It does not remove the guard; there is no setting in which a guarded call goes unannounced.</p><p>The variable deliberately carries no <code>WG_EASY_</code> prefix, which means it reaches every MCP server in the same environment, and — unlike <code>WG_EASY_INSECURE_TLS</code> — a value it does not recognise <strong>stops the server</strong> rather than failing off. See <a href="/guide/approval">Asking a person</a>.</p><h2 id="choosing-the-tools-that-load" tabindex="-1">Choosing the tools that load <a class="header-anchor" href="#choosing-the-tools-that-load" aria-label="Permalink to “Choosing the tools that load”">​</a></h2><p>Read-only mode is one cut, along a line this server drew for you. <code>WG_EASY_ALLOW_TOOLS</code> and <code>WG_EASY_DENY_TOOLS</code> let you draw your own:</p><div class="language-sh"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">WG_EASY_ALLOW_TOOLS</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">essential</span></span>
<span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">WG_EASY_ALLOW_TOOLS</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">list_clients,get_client_config</span></span>
<span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">WG_EASY_DENY_TOOLS</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">delete_client,create_client</span></span></code></pre></div><p>Why bother, when all eleven work: a model chooses the right tool far more reliably from a handful than from a long list, and every tool it can see costs context on every single request. If this is the only MCP server in a session, eleven is fine. If it is one of six, it is not.</p><p><strong>The syntax.</strong> Comma-separated entries. An entry is either an exact tool name or a prefix with a trailing <code>*</code> — <code>list_*</code> matches every tool whose name starts with <code>list_</code>. Entries are trimmed and case-insensitive, empty ones are ignored, and an empty value counts as unset. Nothing else is a pattern: <code>*_x</code> and <code>list_*_x</code> are rejected rather than silently matching nothing.</p><p><strong><code>essential</code></strong> is a curated preset of six:</p><p><code>get_server_info</code>, <code>list_clients</code>, <code>get_client</code>, <code>create_client</code>, <code>enable_client</code>, <code>disable_client</code>.</p><p>Left out on purpose: <code>delete_client</code> and <code>update_client</code>, because both change more than an onboarding step does, and <code>get_client_config</code>, <code>get_client_qrcode</code> and <code>generate_one_time_link</code>, because all three hand a peer&#39;s private key to whoever receives the answer. Handing out a configuration is a normal part of onboarding, so the preset does make that one call harder — deliberately. Name the tool where the same session should also deliver the config.</p><p>It composes — naming a tool alongside it puts that one back, and <code>WG_EASY_DENY_TOOLS</code> takes one away:</p><div class="language-"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span>WG_EASY_ALLOW_TOOLS=essential,get_client_config</span></span></code></pre></div><p><strong>Both together.</strong> <code>WG_EASY_ALLOW_TOOLS</code> decides what is in; <code>WG_EASY_DENY_TOOLS</code> is then subtracted from the result. With only a deny list, everything else stays.</p><p><strong>A name that matches nothing stops the server</strong>, with the offending entry and the list of real names. That is deliberate: the alternative is a tool quietly missing from <code>tools/list</code>, and nobody traces an absence back to an environment variable. The same applies to a pattern that matches no tool.</p><p><strong>With read-only mode</strong>, the write tools are not registered at all, so naming one explicitly in <code>WG_EASY_ALLOW_TOOLS</code> is an error that says so — rather than calling a tool unknown when it plainly exists. A <em>pattern</em> that covers write tools is fine and simply contributes nothing, and <code>WG_EASY_ALLOW_TOOLS=essential</code> narrows to the read half of the preset.</p><div class="tip custom-block"><p class="custom-block-title">It is the same cut, not a second one</p><p>A filtered tool is never registered, so it is absent from <code>tools/list</code> and unknown to <code>tools/call</code> alike — exactly what <code>WG_EASY_READ_ONLY</code> does to a write tool. There is no &quot;hidden but callable&quot; state to reason about.</p></div>`,37)]])}var s=e(a,[[`render`,o]]);export{i as __pageData,s as default};